<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      S'enregistrer - Flashcards
    </title>
    @vite(['resources/css/register.css', 'resources/js/app.js'])
    @vite('resources/js/SignUpButton.js')

  </head>
  <body>
    @include('partials/header')
    <main class="register-main">
      <section class="register-section">
        <h1>
          Inscription
        </h1>

        <form action="{{ route('register') }}" method="POST">
          <div class="form-group">
            <label for="name">Username :</label>
            <input type="text" id="username" name="username" placeholder="Votre username" required />
            <span id="usernameError" style="color:red;"></span>            <!-- Message d'erreur -->
          </div>

          <div class="form-group">
            <label for="password">Mot de passe :</label>
            <input type="password" id="password" name="password" placeholder="Votre mot de passe" required />
            <span id="passwordError" style="color:red;"></span>
          </div>
          <div class="form-group">
            <label for="password_confirmation">Confirmez le mot de passe :</label>
            <input
              type="password"
              id="password_confirmation"
              name="password_confirmation"
              placeholder="Confirmez votre mot de passe"
              required
            />
            <span id="confirmPasswordError" style="color:red;"></span>            <!-- Message d'erreur -->
          </div>
          <button type="submit" id="submitButton" class="btn" style="display:none;">S'inscrire</button>

        </form>
        <p class="redirect">
          Déjà inscrit ? <a href="{{ route('login') }}">Connectez-vous ici</a>.
        </p>
      </section>
    </main>
    @include('partials/footer')
  </body>
</html>
